#include <stdio.h>
#include <time.h>
#include "main.h"
/**
 *print_sign - entry point
 *@n: a letter
 *
 *Description: checks if a letter is in uppercase
 *Return: either 1 or 0
 */
int print_sign(int n)
{
  int time_left = 23;
  unsigned int hours = 0;
  unsigned int minutes = 0;
  unsigned int clock_time, start_time, count_time;
 int  count_down_time = 60;
 start_time = clok();
 while(time_left > 0)
   {

     
   }
return (0);
}

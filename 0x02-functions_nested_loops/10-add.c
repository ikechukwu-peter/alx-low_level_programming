#include <stdio.h>
#include "main.h"
/**
 *add - entry point
 *@c: an int
 *@d: an int
 *
 *Description: checks if a letter is in uppercase
 *Return: either 1 or 0
 */
int add(int c, int d)
{
int result;
result = c + d;
return (result);
}

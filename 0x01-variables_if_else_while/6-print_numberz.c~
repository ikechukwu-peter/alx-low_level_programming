#include <stdio.h>
/**
 *main - entry point
 *Description: printing all the number of base 10
 *Return: Always return 0
 */
int main (void)
{
int c;
for (c = 0; c < 10; c++)
{
putchar(c);
}
putchar('\n');
return (0);
}
